<UserControl x:Class="Vgf.View.AdamManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vgf.View"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="755">
    <Canvas>
        <CheckBox
            Canvas.Left="47"
            IsChecked="{Binding Path=IsManual1.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="5" Canvas.Top="20"
            Text="{Binding Path=Value1.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="152"
            IsChecked="{Binding Path=IsManual2.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="110" Canvas.Top="20"
            Text="{Binding Path=Value2.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="253"
            IsChecked="{Binding Path=IsManual3.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="215" Canvas.Top="20"
            Text="{Binding Path=Value3.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="358"
            IsChecked="{Binding Path=IsManual4.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="320" Canvas.Top="20"
            Text="{Binding Path=Value4.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="463"
            IsChecked="{Binding Path=IsManual5.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="425" Canvas.Top="20"
            Text="{Binding Path=Value5.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="568"
            IsChecked="{Binding Path=IsManual6.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="530" Canvas.Top="20"
            Text="{Binding Path=Value6.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <CheckBox
            Canvas.Left="673"
            IsChecked="{Binding Path=IsManual7.Value, Mode=TwoWay}"/>
        <TextBox
            Canvas.Left="635" Canvas.Top="20"
            Text="{Binding Path=Value7.Value, Mode=TwoWay}"
            Width="100" Height="40" FontSize="20" FontWeight="Bold" 
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
    </Canvas>
</UserControl>
