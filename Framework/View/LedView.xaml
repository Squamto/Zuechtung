<UserControl 
    x:Class="Framework.View.LedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:conv="clr-namespace:Framework.ViewModel"
    mc:Ignorable="d" 
    d:DesignHeight="25" d:DesignWidth="25">
	<UserControl.Resources>
		<conv:BoolToVisibilityConverter x:Key="boolToVisiblity"/>
	</UserControl.Resources>
	<Grid>
		<Grid
			Visibility="{Binding Path=Value, ConverterParameter=false, Converter={StaticResource boolToVisiblity}, FallbackValue=Visible}">
			<Ellipse 
				Visibility="{Binding Path=Value, ConverterParameter=false, Converter={StaticResource boolToVisiblity}, FallbackValue=Visible}"
				Fill="{Binding Path=ColorOn, FallbackValue=Green}"
				Width="10" Height="10"/>
		</Grid>
		<Grid
			Visibility="{Binding Path=IsColorThree, ConverterParameter=true, Converter={StaticResource boolToVisiblity}, FallbackValue=Visible}">
			<Ellipse 
				Visibility="{Binding Path=Value, ConverterParameter=true, Converter={StaticResource boolToVisiblity}, FallbackValue=Hidden}"
				Fill="{Binding Path=ColorOff, FallbackValue=Red}"
				Width="10" Height="10"/>
		</Grid>
		<Grid
			Visibility="{Binding Path=Value, ConverterParameter=true, Converter={StaticResource boolToVisiblity}, FallbackValue=Hidden}">
			<Ellipse
				Visibility="{Binding Path=IsColorThree, ConverterParameter=false, Converter={StaticResource boolToVisiblity}, FallbackValue=Hidden}"
				Fill="{Binding Path=ColorThree, FallbackValue=Yellow}"
				Width="10" Height="10"/>
		</Grid>
	</Grid>
</UserControl>
